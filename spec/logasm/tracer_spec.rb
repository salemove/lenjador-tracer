require "spec_helper"

RSpec.describe Logasm::Tracer do
  it "has a version number" do
    expect(Logasm::Tracer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
